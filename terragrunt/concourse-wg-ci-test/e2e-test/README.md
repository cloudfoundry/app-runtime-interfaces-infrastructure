# WG CI Concourse test instance pipelines

## Add fly target with

`fly login -t wg-ci-test -c https://concourse-test.app-runtime-interfaces.ci.cloudfoundry.org/ -n main`

If already added login with:

```
fly login -t wg-ci-test
```

